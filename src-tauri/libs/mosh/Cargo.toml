[package]
name = "tauri-plugin-mosh"
version = "0.1.0"
authors = ["you"]
edition = "2021"
description = "Mosh client bundler for Android"
links = "tauri-plugin-mosh"

[dependencies]
tauri = { version = "2.0.0" }

[build-dependencies]
tauri-plugin = { version = "2.0.0", features = ["build"] }
